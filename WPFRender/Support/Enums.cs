
namespace WPFRender;

public enum RenderType
{
    SHAPE = 0,
    IMAGE = 1,
    BRUSH = 2,
    LINE = 3,
    DRAWING = 4,
    TRANSFORM = 5,
}

public enum PlayerDirection
{
    NONE = 0,
    UP = 1,
    DOWN = 2,
    LEFT = 3,
    RIGHT = 4,
}
